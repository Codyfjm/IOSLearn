/*
 1
 1）宏不是函数，所以宏的参数不需要加类型说明符，直接写参数名
 2）我们在定义宏的时候，编辑器是如何区分宏名和宏值
    ＃define 空格 宏名 空格
    第一个空格和第二个空格之间的内容会作为宏名
    第二个空格以后的内容会作为宏值
 
 */

#include <stdio.h>

int main(int argc, const char * argv[]) {
   
}
